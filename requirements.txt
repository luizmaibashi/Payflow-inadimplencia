# PayFlow - Previsão de Risco de Crédito
# Dependências do projeto
#
# Pinadas nas versões testadas em 2026-08-08 (152 testes passando, modelo
# treinado nesta combinação). >= permitia build de hoje != build de amanhã
# sem aviso - joblib em particular quebra silenciosamente entre versões de
# scikit-learn divergentes ao carregar o .pkl.

pandas==3.0.1
numpy==2.4.3
matplotlib==3.10.8
seaborn==0.13.2
scikit-learn==1.8.0
imbalanced-learn==0.14.1
joblib==1.5.3
streamlit==1.56.0
fastapi==0.136.1
uvicorn==0.46.0
pydantic==2.13.3
pytest==9.0.3
python-dotenv==1.2.2

# SDKs de LLM ficam em requirements-llm.txt: a suite de testes roda sem eles.
