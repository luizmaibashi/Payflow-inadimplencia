# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv/
.env
.env.*
!.env.example
pip-log.txt
pip-delete-this-directory.txt

# Jupyter
.ipynb_checkpoints

# Streamlit
.streamlit/

# Dataset Home Credit (Kaggle, ~3.3GB extraido - baixar via scripts/baixar_home_credit.md)
data/raw/home_credit/

# Features processadas (~45MB, regeneravel via scripts/camada1_feature_engineering.py)
# Padrao com /* (nao data/processed/) DE PROPOSITO: o git nao entra em
# diretorio excluido, entao a excecao com ! abaixo so funciona se o que esta
# excluido for o CONTEUDO, nao a pasta.
data/processed/*

# EXCECAO: saida de LLM NAO e regeneravel, apesar de morar em data/processed/.
# Medido em 2026-08-08: rodar piloto_camada2.py de novo com o MESMO --seed 42,
# mesma zona cinzenta (2.102 casos) e temperatura 0 produziu memos DIFERENTES —
# 86 validos em vez de 87, e 2 casos rotulados FALHA passaram a recomendar
# APROVAR. Feature engineering e regeneravel (mesmo CSV -> mesmo parquet);
# memo de LLM nao e. Como os labels humanos de data/labels/ descrevem ESTES
# memos, versiona-los junto e o que mantem o par label<->memo de pe (mesma
# causa raiz do debito #28, por outra porta). 194KB.
!data/processed/piloto_camada2_memos.jsonl

# EXCECAO: universo da zona cinzenta (SK_ID_CURR + TARGET real + p_hat).
# E regeneravel em teoria (zona_cinzenta_universo.py), mas exige o dataset
# Home Credit bruto (~3.3GB, gitignored) so disponivel nesta maquina. A demo
# estatica (app/main_v2.py) precisa dele pra rodar em qualquer lugar,
# inclusive Streamlit Cloud - achado em producao 2026-08-12: FileNotFoundError
# porque este arquivo nao existia no clone remoto. 40KB, sem PII (dataset
# anonimizado).
!data/processed/zona_cinzenta.parquet

# Saida gerada por script de verificacao/revisao pontual (AGENTS.md: saida
# regeneravel de script fica fora do git).
scripts/_revisor_verificacao.html
